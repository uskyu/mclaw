# 2026-03-16

- 用户明确说明：主要是给别人搭建大模型网关平台，已经搭建过几十个，对网关、接口格式、模型渠道等概念本身非常熟。
- 重要沟通偏好：当用户让助手测试接口/端点/模型时，优先直接做简单劳动和结果汇报，不要过度解释基础概念，不要绕太多背景说明。
- 本次纠正：用户要的是“直接按指定接口格式测能不能用”，不是概念科普。
- 今日已完成：将本地 workspace 整理并推送到 GitHub 私有仓库 `uskyu/openclaw-workspace-backup`，并配置了每日自动备份 cron（北京时间约 06:22）。
- 今日已完成：飞书协作空间已整理出 `openclaw`、`2API`、`大模型网关合伙`、`Agent 本地备份` 等结构，并新增导航与备份索引文档。
- 浏览器排障关键结论：Ubuntu snap Chromium 不适合 OpenClaw browser 拉起；已安装 Google Chrome `.deb` 版，手动 headless + CDP 可用，但 OpenClaw 仍未成功自动拉起 browser runtime；后续优先考虑 `attachOnly` 方案。
- 新增长期记忆：`https://oreapi.com/` 是用户自己要运营的大模型网关；后续需要我持续帮他做端点测试、接口格式测试、模型通断测试、图片模型出图测试等简单劳动。
- 对 oreapi 这类网关的默认快捷测试流程：给出 base_url + api_key 后，优先做 `/models`、OpenAI/Anthropic 接口通断、指定模型对话/出图测试，并直接回报“能不能用 + 返回结果/报错 + 可访问图片链接（如有）”。
- 新增网关记录：`https://oreapi.com/`，API Key `sk-KSQqnPiklxFUYEDBkOOgXSm07pERgRJyaP6WnJGSKxBH97n2`，用途为“专门的图片 / Gemini 检测测试用网关”。
- 已验证：`gemini-3-pro-image-preview` 可出图，返回为 JSON 包装的 `inlineData(base64)`，需解码后转成 jpg/png 查看。
- 约定：后续用户提到 oreapi 图片测试时，优先使用这个 key 和 `outputs/images/` 目录保存原始返回及解码图片。
